---
title: "For-Each Loop Flowchart"
format:
  html:
    filters:
      - plantuml-filter.lua
---

## For-Each Loop Flow
```plantuml
@startuml
!theme plain
skinparam defaultFontName Arial
skinparam backgroundColor white

start

:Initialize vector\nvector<int> nums = {10, 20, 30};

:Get first element from vector;

while (More elements in vector?) is (yes)
  :Copy current element to loop variable\n(e.g., int num = 10);
  
  :Execute loop body\n(e.g., cout << num);
  
  :Move to next element;
endwhile (no)

:Continue with rest of program;

stop

@enduml
```