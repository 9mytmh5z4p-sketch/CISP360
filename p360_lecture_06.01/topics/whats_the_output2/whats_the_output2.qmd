# What's the Output?

::: notes
Focus: static local variables, basic level, 21 lines
::: 

## Exercise Goals

- Review of code syntax
- Check understanding of syntax
- Understand how static local variables persist across function calls
- Contrast static variables with regular local variables
- Observe that static variables initialize only once

## What to Do

- Using the given code ...
- Trace how static and regular variables behave differently
- Compute the output (MANUALLY)

## What's the Output?

```{.cpp} 
//WhatsTheOutput_Static.cpp
#include <iostream>
using namespace std;

void compareVars();

int main() {
    compareVars();
    compareVars();
    compareVars();
    compareVars();
    return 0;
}

void compareVars() {
    static int staticVar = 0;
    int regularVar = 0;
    staticVar++;
    regularVar++;
    cout << staticVar << " " << regularVar << endl;
}
```

---

::: {.columns}
::: {.column width="75%"}
```{.cpp} 
//WhatsTheOutput_Static.cpp
#include <iostream>
using namespace std;

void compareVars();

int main() {
    compareVars();
    compareVars();
    compareVars();
    compareVars();
    return 0;
}

void compareVars() {
    static int staticVar = 0;
    int regularVar = 0;
    staticVar++;
    regularVar++;
    cout << staticVar << " " << regularVar << endl;
}
```
:::
::: {.column width="25%"}
```{.cpp} 
Output
// 1 1
// 2 1
// 3 1
// 4 1
```
:::
:::